[workspace]
name = "nextflow-minimal-pixi-linux-arm-ubuntu"
description = "Minimal Nextflow pipeline using Pixi for dependency management (Linux ARM Ubuntu)"
version = "1.0.0"
authors = ["Tracer Bioinformatics Team"]
channels = ["conda-forge", "bioconda"]
platforms = ["linux-aarch64"]

[dependencies]
nextflow = "24.10.0"
openjdk = "17.0.11"
python = "3.12"

grep = "*"
tar = "*"
coreutils = "*"
findutils = "*"

fastqc = "0.12.1"
star = "*"
samtools = "1.21"
bwa = "*"
fgbio = "*"

[tasks]
pipeline = "nextflow -log logs/nextflow.log run main.nf --outdir results"
clean = "rm -rf results logs work test_results custom_results .nextflow*"

[feature.dev.dependencies]
nextflow = ">=24.10.0"

[environments]
default = ["dev"] 