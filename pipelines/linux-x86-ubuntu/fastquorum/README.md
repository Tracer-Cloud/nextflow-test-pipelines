# Ubuntu Linux x86_64 Nextflow Fastquorum Pipeline (pixi)

This directory contains a bash script that sets up and runs a Nextflow fastquorum pipeline using pixi.

## Quick Start

```bash
# Run the setup and pipeline script
./run.sh
```
