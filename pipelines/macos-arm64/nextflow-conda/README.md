# macOS arm64 Nextflow Pipeline (pixi)

This directory contains a bash script that sets up and runs a Nextflow pipeline using conda.

## Quick Start

```bash
# Run the setup and pipeline script
./run.sh
```
