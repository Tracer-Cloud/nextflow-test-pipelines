# Codespaces Fastquorum Pipeline

This directory contains a bash script that sets up and runs a Nextflow fastquorum pipeline using Pixi in GitHub Codespaces.

## 🚀 Quick Start

```bash
# Run the setup and pipeline script
./run.sh
```
